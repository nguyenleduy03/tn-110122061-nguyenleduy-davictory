package com.victory.DAVictory.enums;

public enum MediaType {
    AUDIO,      // File âm thanh (.mp3, .wav, .ogg)
    IMAGE,      // Hình ảnh (.jpg, .png, .svg)
    VIDEO,      // Video (.mp4, .webm)
    DOCUMENT    // Tài liệu (.pdf, .docx)
}
